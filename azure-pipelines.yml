# Maven
# Build your Java project and run tests with Apache Maven.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/java

trigger:
- azure-pipelines-07232023 # or whichever branch you want to trigger the pipeline on

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: |
        which java
        java -version
        echo JAVA_HOME_8_x86=$JAVA_HOME_8_x86
        echo JAVA_HOME=$JAVA_HOME
        javac
      displayName: 'Show Release Number'
# Java tool installer v0
# Acquire a specific version of Java from a user-supplied Azure blob or the tool cache and sets JAVA_HOME.
- task: JavaToolInstaller@0
  inputs:
    versionSpec: '20'
    jdkArchitectureOption: 'x64'
    jdkSourceOption: 'PreInstalled'
    JAVA_HOME_13_X64 : "/usr/lib/jvm/java-20-openjdk-amd64"

- script: 'java -version'
  displayName: 'Display Java Version'
  
- task: Maven@3
  inputs:
    mavenPomFile: 'pom.xml'
    mavenOptions: '-Xmx3072m'
    javaHomeOption: 'JDKVersion'
    jdkVersionOption: '20'
    jdkArchitectureOption: 'x64'
    publishJUnitResults: false
    testResultsFiles: '**/surefire-reports/TEST-*.xml'
    goals: 'clean install test'


